---
apiVersion: v1
kind: Service
metadata:
  name: annotation-converter-webhook
  namespace: d8-monitoring
  {{- include "helm_lib_module_labels" (list . (dict "app" "annotation-converter-webhook")) | nindent 2 }}
spec:
  selector:
    heritage: deckhouse
    module: {{ .Chart.Name }}
    app: annotation-converter-webhook
  ports:
  - name: https
    port: 443
    targetPort: 8080
